# 训练数据探索报告

> **历史报告，部分重要结论已修正（2026-09-18）。** 当前数据分析见 [reports/data_audit.md](reports/data_audit.md)，问题定位见 [reports/review_notes.md](reports/review_notes.md)。原F10将两UE合并后与单UE预算比较并遗漏截断误差；F11的FFT轴错误；F12的QAM逆映射少了 `/2` 且p10聚合口径错误。以下数值保留供追溯，不能作为当前模型上下界或方案排除依据。

> 基于 `data_train/H_train.npz`（100,000 样本 × [2 UE, 2 RX, 16 TX, 144 SC]，real/imag 以 **float16** 存储）。
> 复现：`python data_explore.py`（CPU，约 2 分钟），图表见 `figs/`。

## 数据格式

- shape `[100000, 2, 2, 16, 144]` complex64（由 fp16 real/imag 转换而来），fp16 量化范围 ±6.8
- 评测侧采样方式（modelEval.py）：样本从 H_train 有放回抽取，两用户 SNR 独立均匀于 [-20, 20] dB

## 关键发现

### F1. 瑞利型信道，无视距分量，各天线/子载波统计均衡
- 每元素平均功率 E|h|² = **1.79**（不是 CN(0,1)，做 SNR 标定时要注意）
- 逐元素检验无确定性分量（0% 元素超 4σ，纯随机相位）；|h| 直方图呈瑞利型，1% 分位 0.11（存在深衰落）
- 16 根发射天线、2 根接收天线平均功率完全一致（1.786~1.792）

### F2. 时延域高度紧凑：指数衰减 PDP，前 4 个抽头含 95% 能量
- PDP 从 tap0 起每抽头约 -4.7dB 指数衰减，-20dB 以上只有 ~6 个抽头（`figs/fig2_delay.png`）
- 逐链路统计：**前 4 个 IFFT 时延抽头中位数能量占比 95.3%**（10 分位 84.2%），前 8 抽头 96.8%，之后趋平（剩余为量化噪声底）

### F3. 频域高度平滑
- 子载波间隔 1 的相关系数 0.999，间隔 24 约 0.76，间隔 48（一个子带）约 **0.65**，可观测范围（±72）内不低于 0.5（`figs/fig3_freqcorr.png`）
- 同一样本内三个子带（48 SC）的增益波动 std/mean 中位数仅 **0.105**

### F4. 发射阵列为双极化 8 阵位结构（重要！）
- 16×16 天线相关矩阵呈清晰双块结构（`figs/fig4_spatial.png`）：天线 0-7 与 8-15 各自块内强相关（相邻 ~0.9，随阵位距离衰减），**两块之间几乎零相关**
- 即经典 3GPP 双极化 ULA：8 个空间阵位 × 2 个极化（天线 p → 阵位 p%8，极化 p//8）
- 推论：空间自由度 ≈ 每极化的方位波束 + 交叉极化的近独立维度；波束域反馈天然按 (极化, 波束) 组织

### F5. 双流有意义，但第一波束主导
- 每用户每子载波 SVD：E[s1²] = 45.2，E[s2²] = 12.2；s2/s1 中位数 **0.515**
- 仅 9.5% 的 (样本，子载波) 满足 s2/s1 < 0.3（单流主导）

### F6. 用户间天然低相关：MU 配对是默认可行项
- 用户 1 主波束能量漏到用户 2 信道子空间的比例：中位数 **0.151**，90 分位 0.405，**仅 4.4% 的 (样本，子载波) 漏泄 > 0.5**
- 向量化信道互相关 |ρ| 中位数 0.24，90 分位 0.48（`figs/fig6_users.png`）

### F7. 用户间大尺度增益基本对称
- 同样本两用户信道总增益之比：中位数 1.02，5~95 分位 [0.21, 4.96]（约 ±7dB 内）

### F8. SNR 标定：名义 SNR ≠ 波束成形后 SNR
- 发送功率归一化为每子载波全阵 1；全功率单流 SU 时，接收波束增益 ≈ s1²（中位约 40 ≈ 16dB）
- 即名义 -20dB 的弱用户，匹配波束后实际约 -4dB——**低速率传输仍然可解**，这正是公平分（第 10 百分位）的提升空间

## 对系统设计的推论

| 设计点 | 数据支持的方案 |
|---|---|
| f_enc 反馈压缩 | 4608 复数/用户 压到 96 绰绰有余：时延域（前 4-8 抽头）+ 双极化波束域联合压缩，理论 DOF 每链路仅 ~4 复抽头 × 32 链路 |
| 预编码粒度 | 子带（48 SC，ρ≈0.65）合理；逐子载波增益有限（F3） |
| 速率自适应主轴 | 按**用户/SNR** 自适应为主（F8），按子带自适应收益有限（F3：子带内波动中位 0.105） |
| MU vs SU | MU 空分复用为默认模式（F6：>95% 样本子载波漏泄 < 0.5）；SU 回退只做小概率尾部处理 |
| SU 层数 | 双流常有必要（F5：s2/s1 中位 0.5），高 SNR 时 SU-2 层可能优于 MU |
| 训练 | 两用户 SNR 独立 → 弱用户样本需要专门加权，与公平分（10 分位）对齐 |

---

# 第二轮探索：压缩下界与方案上界（`data_explore2.py`）

## F9. 时延抽头位置固定，无需学习抽头基
- 每链路能量峰抽头：tap0 占 69.5%、tap1 占 22.3%、tap2 占 5.1%；能量质心 0.72±0.81（`figs/fig7_structure.png`）
- → 时延基固定锚定在 tap0 附近，直接用固定 DFT 基即可，不需要可学习抽头位置

## F10. 反馈压缩的信息下界（PCA 谱，时延域 2048 实维）
- 能量占比：90% 需 123 维、95% 需 188 维、99% 需 387 维
- **在 96 复数（192 实维）反馈预算下，PCA 重建 NMSE = 4.8%（-13.2 dB）；96 实维 13.9%**
- → 这是 f_enc 重建精度的理论参考线；上行噪声（snr_dl-10dB）会进一步压低有效维度

## F11. 方位角近各向同性，用户可分性来自随机角度差
- 8 阵位 DFT 角度谱归一化熵 0.979（1.0=完全各向同性）；两用户主方位角一致率 16%（随机水平 12.5%）
- 双极化功率均衡（pol0 份额 0.497）
- → F6 观察到的 MU 低干扰本质是"用户角度随机散开 + 16 天线孔径分辨"，设计上无需依赖任何固定角度结构

## F12. 传输方案在赛题评分下的上界对比（完美 CSI + 蒙特卡洛实测 BER 表）

| 方案 | 效率分 | 公平分 | 最终分(0.7/0.3) |
|---|---:|---:|---:|
| MU-ZF 固定 8bit/RE（≈基线架构的隐含模式） | 56.92 | 52.36 | 55.55 |
| MU-ZF + 调制阶数自适应 | 57.17 | 52.45 | 55.75 |
| SU 贪心（服务好用户） | 54.27 | 50.00 | 52.99 |
| **MU-ZF + 跨 RE 理想信道编码** | **74.50** | 52.12 | **67.78** |
| SU 全功率 + 理想编码 | 66.37 | 50.00 | 61.46 |
| 理想编码 + MU/SU 逐样本 oracle 切换 | 75.16 | 50.00 | 67.61 |

关键读数（完美 CSI、un/code 差异即系统优化空间）：
1. **跨 RE 编码是最大单一杠杆：效率分 +17.6**（56.9→74.5）。低 SNR 用户瓶颈在"分数比特率"——容量 0.3 bit/RE 时任何整数调制都无解，必须靠跨子载波扩频/编码
2. **调制阶数自适应几乎无增益**（+0.25）：0.5 保底分规则下，BER<40% 的 256QAM 通常仍优于降阶
3. **SU 回退是公平分陷阱**：未服务用户恰好 50 分，把 oracle 的 p10 钉在 50 → 最终分反而不如 always-MU（67.61 < 67.78）
4. **公平分 p10≈52 接近物理墙**：p10 用户 ≈ snr -17dB 以下，其容量本身只支持 μ≈50-55；只能靠向弱用户功率倾斜小幅改善
5. 数值校验：null 空间 ZF 增益中位数 23.4（SU 匹配波束 54.6），合理

## 修订后的优化优先级

1. **速率自适应的跨 RE 编码**（第一优先）：发射端按信道条件发 B<1152 比特并扩频到全部 RE，接收端相应解扩——即学习式信道编码；用 5 个 ctrl 比特告知 B（32 档）
2. **永远 MU**：不要 SU 回退；对配对不利的少数样本做功率倾斜而非切换
3. **f_enc 按 F10 下界对齐**：时延域 + 波束域结构化压缩，192 实维目标 NMSE ≈ 5%
4. 跳过调制阶数自适应；公平分不再单独投入（接近物理上限），资源全部给效率分（权重 0.7 且空间大）
