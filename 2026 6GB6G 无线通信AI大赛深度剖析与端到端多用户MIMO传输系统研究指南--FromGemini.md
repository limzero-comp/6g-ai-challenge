#   
# 2026 6G/B6G 无线通信AI大赛深度剖析与端到端多用户MIMO传输系统研究指南--FromGemini  
## 6G与AI原生空口的演进背景与标准化进程  
通信与人工智能（AI）技术的深度融合已被学术界与工业界公认为第六代移动通信（6G）及其后续演进版本（B6G）的最重要发展方向之一。在现有的5G系统中，物理层的数据传输链路通常由多个独立设计的信号处理模块串联而成。信道编码、调制、预编码等构成发射端，而信道估计、均衡、解调、信道解码等构成接收端。这种模块化架构虽然允许各个模块在其自身功能范围内被优化至理论极限，但模块间的孤立性导致局部最优并不等于整个链路的全局最优。随着无线环境的日益复杂和海量连接需求的涌现，传统算法在系统级协同上的性能瓶颈逐渐显露。  
从国际标准化组织的进程来看，3GPP在Release 17中已经开始了AI赋能无线接入网（RAN）的基础性探讨，而在Release 18（5G-Advanced）中，则正式确立了“AI原生空口”（AI/ML for NR Air Interface）的研究项目。该项目重点评估将AI引入物理层与MAC层后所带来的性能增益与标准化影响，其核心用例涵盖了信道状态信息（CSI）反馈增强、波束管理以及高精度定位等领域。进入Release 19后，3GPP进一步扩大了AI在移动性管理和网络调度中的应用范围，并致力于构建能够支撑未来6G网络的AI原生架构底座。  
本次由IMT-2030 (6G) 推进组主办、广东欧珀移动通信有限公司（OPPO）承办的“2026 6G/B6G Wireless Communication AI Competition”正是在这一宏大背景下发起。大赛旨在向社会各界推广6G/B6G的先进愿景，系统性地探讨将AI驱动的数据传输链路与控制机制进行端到端（End-to-End）联合设计的可行性与性能上限，以期全面推动智慧6G的技术突破。  
## 赛题任务拆解：多用户MIMO端到端传输系统  
本次大赛的核心命题为“6G/B6G内生AI：多用户MIMO端到端传输系统设计”。在多用户多输入多输出（MU-MIMO）场景下，基站利用同一时频资源通过空分复用同时服务多个用户，系统容量可实现成倍增长。然而，基站必须在空域上协调多个用户之间的资源分配，包括用户配对、功率分配、预编码抗干扰设计等。当传输链路本身由AI驱动时，控制机制面临的调度复杂度将呈指数级上升。  
**系统架构与链路交互机制**  
参赛者需要设计部署于三个核心节点的AI模型，这三个模型共同构成了完整的端到端闭环系统：  
1. **终端编码器 $f_{enc}(\cdot)$**：部署于各用户终端，负责将本地的下行信道矩阵 $H_i$ 与信噪比信息 $snr_{dl\_i}$ 进行有损压缩，生成上行反馈信号 $U_i$。该信号通过有损的上行加性噪声信道传输至基站，形成接收信号 $I_i$。  
2. **基站发射机 $f_{tx}(\cdot)$**：作为整个系统的数据处理与调度中枢，汇集所有用户的反馈信息集合 $I$ 与待传输数据比特流 $b_{data}$，处理生成下行数据发送信号 $X$。同时，发射机必须生成用于收发对齐的 $K$ 比特下行控制信令 $b_{ctrl}$。  
3. **终端接收机 $f_{rx}(\cdot)$**：部署于各终端，根据本地的下行接收信号 $Y_i$、理想已知的信道信息 $H_i$、下行控制信令 $b_{ctrl}$ 及信噪比，恢复出数据比特的对数似然比（LLR）$c_{data\_i}$，并最终通过硬判决输出预测比特。  
**系统物理参数约束**  
赛题在硬件维度与信道资源上施加了严格的物理约束，逼近真实的物理层极限。详细的系统配置参数如表1所示。  

| 参数名称                    | 物理含义与约束数值                                 |
| ----------------------- | ----------------------------------------- |
| 下行数据频域子载波数 ($S_{dl}$)   | 144个子载波，用于承载基站到终端的下行数据信号。                 |
| 用户终端数 ($N_{ue}$)        | 2个终端，考察最基础的多用户配稳与干扰抑制能力。                  |
| 下行数据发送天线数 ($N_{tx}$)    | 16根天线，部署于基站侧。                             |
| 单个终端下行接收天线数 ($N_{rx}$)  | 2根天线，形成 $16 \\times 2$ 的下行信道矩阵。           |
| 下行控制比特数 ($K$)           | 仅允许无损传输5个比特，控制信令容量极其受限。                   |
| 多用户最大传输总比特数 ($B_{max}$) | 2304个比特，代表系统的理论峰值吞吐量。                     |
| 下行信噪比 ($snr_{dl}$)      | -20 dB 至 20 dB，覆盖了从极度恶劣到理想的信道环境。          |
| 上行反馈频域子载波数 ($S_{ul}$)   | 96个子载波，且为单发送单接收天线配置，反馈资源呈现显著瓶颈。           |
| 上行信噪比 ($snr_{ul}$)      | $snr_{dl} - 10$ dB，上行链路质量始终劣于下行，反馈信号极易受损。 |
  
（表1：MU-MIMO端到端系统物理参数配置表）  
**双维评价体系与目标函数博弈**  
大赛采用了“双维双榜”的评价机制，总成绩由A榜（客观量化榜）占80%权重与B榜（创新潜力榜）占20%权重组合而成。A榜的客观量化得分极具战略指导意义，系统同时考察传输的效率与用户间的公平性。单用户传输得分公式定义为：  
$$\mu = 100 \times \frac{ B_c + (B_{\max} - B)\times 0.5 }{ B_{\max} }$$  
其中，$B_{\max}$ 为最大传输比特数，$B$ 为实际传输比特数，$B_c$ 为正确传输的比特数。这一公式蕴含了深刻的通信博弈逻辑：系统对放弃传输的比特（即 $B_{\max} - B$）给予了0.5的权重补偿。这意味着，当信道条件极度恶劣（如SNR处于 -20 dB）时，盲目以最大比特数进行传输将导致极高的误码率，最终得分可能远低于主动降低传输速率甚至完全不传输的保守策略。  
进一步地，A榜最终客观得分由70%的“效率分”（所有用户分数的均值）和30%的“公平分”（所有用户分数按升序排列后的第10百分位值）构成。这种得分机制迫使参赛者在追求系统全局极致吞吐量的同时，绝不能牺牲处于网络边缘或面临恶劣信道条件用户的服务质量。  
## 编码器设计：CSI反馈压缩与量化重建的深度学习范式  
在FDD（频分双工）架构的MIMO系统中，下行信道状态信息的获取严重依赖于终端的估计与上行反馈。鉴于基站天线数量的激增与子载波的宽带特性，原始CSI矩阵维度极为庞大。传统的基于码本（Codebook）或压缩感知（Compressive Sensing, CS）的方法在极低压缩率下难以保证信号的恢复精度，逐渐无法满足海量MIMO系统的需求。因此，利用深度学习模型进行CSI反馈压缩已成为3GPP Release 18及学术界的主流共识。  
**从CsiNet到多分辨率与注意力机制的演进**  
自首个基于自编码器架构的CSI反馈模型CsiNet问世以来，学术界涌现了大量旨在降低计算复杂度并提高恢复精度的网络架构。在本赛题中，输入信道矩阵 $H_i \in \mathbb{C}^{2 \times 16 \times 144}$ 需被映射为维数极低的上行反馈信号 $U_i \in \mathbb{C}^{96}$，这对特征提取的高效性提出了严苛挑战。  
多分辨率卷积网络（CRNet）通过引入多分辨率卷积核来提取多尺度特征。其核心思想是将传统的大尺寸二维卷积核（如 $9 \times 9$）分解为两个一维卷积核（$1 \times 9$ 与 $9 \times 1$）的串联，并在网络中同时保留小尺寸卷积核（如 $3 \times 3$）的并行路径。这种架构在提取大感受野全局特征的同时，有效捕捉了局部细节，同时其非对称和多分支设计能够显著缓解高度压缩引起的网格效应伪影。  
空洞卷积网络（DCRNet）则提供了一种面向终端设备算力受限场景的轻量化解决方案。该网络利用膨胀卷积（Dilated Convolution）在不增加网络参数量的情况下成倍扩大感受野，使其能够捕获更广泛的空域相关性。实验表明，DCRNet在保持极低浮点运算次数（FLOPs）的同时，能够达到与高复杂度网络相媲美的归一化均方误差（NMSE）表现。  
随着自然语言处理领域的技术迁移，基于注意力机制的Transformer网络（如TransNet）被引入到CSI反馈中。由于无线信道在频域和空域上具有长距离的相关性与稀疏性，TransNet通过自注意力机制（Self-Attention）能够有效捕捉子载波与天线间的全局依赖关系，在应对如COST2100等复杂室内外信道数据集时，展现出了超越传统卷积神经网络的恢复精度。  
**应对反馈链路约束的量化与在线学习**  
实际的通信系统无法传输无限精度的浮点数，特征信号必须经过量化处理。QCRNet（Quantization Adaptor for Bit-Level CSI Feedback）通过在编码器末端引入量化适配器，将连续的特征向量映射为离散的比特流，解决了低比特量化带来的严重精度损失问题。此外，通信环境是动态变化的，预训练的固定模型在面对全新信道分布时往往性能骤降。CFNet（Continuous Online Learning-based CSI Feedback）提出了一种基于连续在线学习的框架，通过无监督的微调技术缓解了在线更新过程中的灾难性遗忘（Catastrophic Forgetting）问题，为应对动态变化的 $snr_{dl}$ 提供了理论支撑。  
以下表格整理了可用于本赛题编码器设计的代表性开源架构库，这些代码库提供了完整的数据预处理、模型定义与性能基准：  

| 模型架构名称 | 核心技术特征与优势 | 适用场景与赛题应用建议 | GitHub 开源代码库指引 |
| ---------------- | ----------------------------------------------------- | ------------------------------------- | ------------------------------------------------- |
| CsiNet / CsiNet+ | 奠基性的自编码器架构，使用基础卷积与全连接层实现降维与重建。 | 作为Baseline模型，用于跑通端到端训练流水线，验证数据流动的正确性。 | https://github.com/SeokhyunJeong/CsiNet-Pytorch |
| CRNet | 多分辨率并行路径，利用分解卷积核（$1\\times9$ 和 $9\\times1$）兼顾全局与局部特征。 | 在控制终端模型参数量的前提下，大幅提升压缩特征的表征能力。 | https://github.com/Kylin9511/CRNet
[cite: 12, 13] |
| TransNet | 引入Transformer的全注意力机制，捕获长距离空频相关性。 | 当信道多径效应明显且具有复杂频域选择性衰落时，可作为强力编码器。 | https://github.com/Treedy2020/TransNet
[cite: 15] |
| DCRNet | 引入非对称空洞卷积（Dilation），不增加参数量即可扩大感受野。 | 适用于极其严格的推理时间限制下的轻量化网络设计。 | https://github.com/tangshunpu/DCRNet
[cite: 14] |
| QCRNet | 集成量化适配器，支持特定比特宽度的特征量化。 | 适用于需要将连续特征离散化，或处理赛题中上行链路的数字调制映射约束。 | https://github.com/zhang-xd18/QCRNet
[cite: 16] |
| CFNet | 连续在线学习机制，抵抗灾难性遗忘。 | 可借鉴其对抗多变信道分布的思路，提升模型在未见信噪比区间内的鲁棒性。 | https://github.com/zhang-xd18/CFNet
[cite: 17] |
  
（表2：面向CSI特征提取与压缩的开源深度学习模型对比表）  
## 发射机设计：MU-MIMO预编码与资源调度的AI展开范式  
基站侧发射机 $f_{tx}(\cdot)$ 是决定系统性能的枢纽。在获取了由 $f_{enc}$ 压缩并受到上行噪声 $N_{ul\_i}$ 污染的联合反馈 $I$ 后，基站需要同时完成多用户配对决策、预编码矩阵计算、功率分配以及控制信令生成。  
**传统预编码算法的计算瓶颈**  
在经典通信理论中，MU-MIMO的下行预编码通常被建模为加权和速率（Weighted Sum Rate, WSR）最大化问题。由于信道间的相互耦合，该问题在数学上属于非凸且NP-hard问题。工业界与学术界常采用加权最小均方误差（WMMSE）算法或分数规划（FP）算法通过交替优化来逼近最优解。  
然而，WMMSE算法存在极其严重的计算效率瓶颈。其每一次迭代都需要进行复杂的高维矩阵求逆操作，并且为了满足功率约束，还需对拉格朗日乘子进行耗时的二分法搜索。在天线数量庞大的大规模MIMO系统中，WMMSE往往需要数十次串行迭代才能收敛，这完全无法满足实际系统（以及本赛题1000秒内推断完毕）的低延迟硬性约束。  
**深度展开（Deep Unfolding）与图神经网络的融合**  
为突破传统迭代算法的瓶颈，深度展开（Deep Unfolding / Unrolling）技术成为最具潜力的研究方向。其核心思想是将已知迭代算法（如WMMSE或梯度下降）的每一次更新步骤映射为深度神经网络的一个层（Layer），并将算法中的固定步长、正则化项或惩罚因子转化为网络中可通过数据驱动优化的可学习参数。  
通过深度展开，网络能够以极少的层数（通常为4到8层）达到甚至超越原算法数十次迭代的性能。例如，LAGD（Learned Accelerated Gradient Descent）算法和基于投影梯度下降（PGD）的无矩阵求逆展开网络，将复杂的拉格朗日乘子求解替换为轻量级的向量级操作，实现了计算复杂度数量级式的下降。UI-DUFP（Uncertainty-Injected Deep Unfolded Fractional Programming）框架则在展开分数规划迭代的同时，通过在训练阶段注入采样的信道不确定性噪声，并优化基于分位数的损失函数，极大地提升了预编码在不完美CSI环境下的鲁棒性。  
此外，将图神经网络（GNN）与深度展开融合展现了极大的应用前景。系统可以将基站和多用户视为图中的节点，将它们之间的干扰信道视为边，利用图卷积网络（GCN）处理多用户间的干扰拓扑，从而学习分布式的功率分配和波束赋形策略。此类架构（如GCN-WMMSE）天然具备对用户数量和排列置换的不变性，泛化能力极强。  
**消除相位模糊：复射影空间与流形学习**  
在基于数据驱动的MU-MISO/MIMO预编码设计中，存在一个容易被忽视的物理陷阱：信道和预编码向量的“全局相位旋转不变性”。预编码系统的核心性能指标（如接收端的信号功率与用户间干扰功率）仅依赖于信道向量与预编码向量内积的幅值，而与它们整体的绝对相位无关。传统的神经网络通常将复数参数化为实部与虚部的拼接，或者幅值与相位的拼接，这迫使神经网络花费大量参数隐式地去学习这种相位不变性，极大地增加了网络的学习难度并产生了严重的冗余。  
近期前沿研究表明，通过将信道与预编码向量映射到复射影空间（Complex Projective Space, $CP^n$）或格拉斯曼流形（Grassmannian manifold）上，直接在流形空间中定义物理上不同的信道-预编码对，能够彻底消除由全局相位变化引起的模糊性。引入流形学习与几何感知的深度框架，能够在大幅降低训练难度的同时，实现预编码精度和收敛速度的显著提升。  
**MU/SU 动态切换与控制信令设计**  
赛题中允许基站生成 $K=5$ 个控制比特（即32种独立状态），这为收发机的高效模式协同提供了关键通道。在多用户场景下，当基站通过解码反馈信号发现两个用户的信道高度相关时，强行进行MU-MIMO配对传输会导致严重的相互干扰（串扰），使得信号被干扰完全淹没。在这种极端信道条件下，将所有功率和时频资源退回倾斜给单一用户（SU模式）往往能够获得更高的系统吞吐量与得分。  
因此，发射机网络不能仅仅是一个单纯的信号调制器，它还应当集成类似分类代理的功能。基于重构出的信道正交性、评估的下行SNR以及不同策略下的预期风险，发射机需从预置的模式库中输出最佳的资源分配方案，并通过这5个控制比特告知终端接收机。合理的信令定义方案例如：  
* 使用1个比特指示当前为MU空分复用模式还是SU回退模式。  
* 使用1个比特在SU模式下指示当前实际服务的是用户1还是用户2。  
* 使用剩余3个比特指示当前采用的调制阶数、信道编码码率（MCS）策略或特定的频率资源分配方案，确保终端的接收机模块能够进行精准的结构切换。  

| 开源算法库名称 | 核心解决问题与算法机制 | 在本次竞赛中的应用价值 | GitHub 开源库溯源 |
| -------------------- | -------------------------------------- | ------------------------------------------- | ----------------------------------------------- |
| WMMSE-Deep-Unfolding | 将迭代WMMSE算法映射为深度网络，利用投影梯度下降（PGD）取代矩阵求逆。 | 解决传统预编码计算过慢问题，实现快速且可导的功率与波束分配。 | https://github.com/lpkg/WMMSE-deep-unfolding |
| GCN-WMMSE | 融合图卷积网络（GCN），处理多用户干扰拓扑。 | 能够高效处理复杂的多用户信道干扰关系，且具备用户置换不变性。 | https://github.com/lsky96/gcnwmmse |
| LAGD | 学习加速梯度下降算法，无需标签数据进行无监督训练更新。 | 提供了另一种摆脱WMMSE性能上限束缚的隐式优化法则，提升发射机生成质量。 | https://github.com/XiaGroup/LAGD |
| DDQN_BeamSelection | 深度强化学习与展开网络结合，进行波束选择。 | 强化学习思维可直接用于 $b_{ctrl}$ 中离散模式的选择（如MU与SU的切换）。 | https://github.com/hqyyqh888/DDQN_BeamSelection |
  
（表3：面向预编码优化与深度展开技术的开源资源列表）  
## 接收机设计：神经接收机与软判决信息的深度集成  
在传统接收端架构中，接收信号需要依次经过信道估计、信道均衡、解调器与信道译码器等多个独立模块。随着计算能力的提升，深度学习在此领域的应用已从早期的单一模块替代，全面走向了一体化的“神经接收机”（Neural Receiver）范式。  
**DeepRx神经接收机架构**  
针对基于正交频分复用（OFDM）体制的无线系统，DeepRx架构提供了一种极为成功的神经接收机基准。其核心理念是舍弃传统的线性最小均方误差（LMMSE）信道均衡，通过全卷积神经网络（FCNN）或具备残差连接的网络，直接将带导频的二维时频接收网格（包含 $Y_i$）与已知的信道矩阵（$H_i$）映射为发送信息特征。为了应对复杂多径与动态衰落，较新的神经接收机变体进一步引入了自注意力机制（如Vision Transformer），在时间和频率二维尺度上联合捕获信道的瞬态变化。相较于传统算法，神经接收机在处理信道非线性失真及低信噪比环境下的鲁棒性具有跨代别的优势。相关的PyTorch开源实现可以参考 https://github.com/G-ALI007/DeepRx-OFDM-PyTorch。  
**LLR输出与Neural Min-Sum算法**  
赛题在接收机 $f_{rx}$ 接口设计上提出了特定要求：网络输出并非直接的二进制比特流，而是对应接收数据比特的对数似然比（LLR）$c_{data\_i} \in \mathbb{R}^B$。这种软信息输出意味着网络不能通过简单的Softmax或Sigmoid函数强行输出硬判决的0或1，而必须提供一个反映概率置信度的连续实数，以支持后续进行更精准的软判决信道解码或直接的阈值硬判决。  
在网络设计上，接收机的输出层应当保留为不带非线性激活函数的线性层（Logits输出），并采用二元交叉熵（Binary Cross-Entropy, BCE）作为损失函数。从信息论角度出发，当BCE损失函数被充分最优化时，神经网络输出的前置激活值天然等价于信源的后验概率对数似然比（LLR）。  
为了进一步压榨解码性能并提升软信息的置信度精度，可以借鉴Neural Belief Propagation (Neural BP) 或分层最小和（Layered Min-Sum）算法，将传统的图模型解码迭代过程直接展开为神经网络层。相比传统的泛洪式消息传递，基于分层最小和架构的神经网络能够在极少的可训练权重和极低的迭代次数下，加速收敛并实现接近最大似然（ML）解码的极致性能，显著提升恶劣SNR下的比特恢复率。  
## 端到端联合优化：DeepJSCC与直通估计器（STE）的突围  
模块的单独卓越并不意味着系统的全局最优。要使上述设计的编码器、发射机和接收机发挥出最大系统级效用，必须实现真正的端到端（E2E）联合训练。即将发送端信号处理、物理噪声信道传输以及接收端信号恢复视为一个整体的广义自编码器系统，通过梯度下降联合优化所有参数。  
这一思路与近年来学术界兴起的深度联合信源信道编码（DeepJSCC）以及语义通信理念高度一致。DeepJSCC打破了传统的香农信源信道分离定理的桎梏，不再依赖于固定的编码比特流作为中间媒介，而是将特征直接映射为能在信道中抵抗特定噪声分布的连续符号。对于本赛题的上行反馈链路而言，编码器可以直接通过类似DeepJSCC的机制，在给定信噪比下自动学习最优的有损特征映射。  
**突破不可导瓶颈：直通估计器（STE）**  
端到端训练中的最大技术障碍在于离散化操作导致的“梯度消失”或“不可导性”。在本赛题的系统框架中：  
1. 发射机 $f_{tx}$ 必须生成离散的控制比特 $b_{ctrl} \in \{0,1\}^K$。  
2. 若系统内部保留了传统的调制约束，连续特征需要被映射到离散的星座点上（如QAM或BPSK星座图）。  
这些量化或二值化操作（例如采用 sign() 或 round() 函数）的导数在数学上几乎处处为零。这将彻底切断基于链式法则的反向传播梯度流，导致发射机前端和终端编码器无法更新参数。  
业界解决这一数学死锁的主流利器是**直通估计器（Straight-Through Estimator, STE）**。通过利用PyTorch等框架自定义 autograd 函数，STE机制能够在前向传播（Forward Pass）时执行严格的数学量化操作（例如生成精确的离散二进制比特），而在反向传播（Backward Pass）时，人为地将梯度“绕过”（Bypass）该不可导层，直接将后一层的梯度原封不动地传递给前一层；或者采用一个可导的平滑替代函数（如缩放版Tanh函数）的梯度来近似真实的梯度。  
借助STE技术，整个多用户MIMO系统从终端反馈开始，经过基站的联合调度与发送，再到下行物理信道传输与终端的解调输出，其整个闭环将被彻底打通。梯度能够稳定地从 $f_{rx}$ 穿越下行信道回传至 $f_{tx}$，甚至穿越具有高度噪声的上行反馈链路一直回传至 $f_{enc}$，从而真正实现物理层网络中AI组件的内生协同与全局最优化。  
**全局仿真与端到端开源工具箱**  
为了便于搭建完整的端到端仿真环境，可借助业界成熟的通信系统仿真框架。以下两款基于Python/PyTorch的工具箱是构建基线系统的最佳选择：  

| 端到端仿真库名称 | 核心功能与架构支持 | 在本次大赛中的应用角色 | GitHub/官方来源 |
| --------------- | -------------------------------------------------------------------- | ------------------------------------------- | -------------------------------- |
| Sionna (NVIDIA) | 提供了物理层所需的各种信道模型、OFDM调制解调及多天线MIMO仿真模块。内建对神经接收机（Neural Receiver）的原生支持。 | 利用其现成的物理层计算图构建信道传播模型，直接替换赛方占位代码，加速整体框架的成型。 | https://github.com/NVlabs/sionna |
| Kaira | 专门面向通信系统研究设计的PyTorch工具箱，原生支持DeepJSCC等基于深度学习的联合源信道编码及前向纠错码算法。 | 帮助搭建端到端的训练Pipeline，特别适用于处理包含反馈信道的联合优化及模块对接。 | https://github.com/ipc-lab/kaira |
  
（表4：面向6G/B6G物理层系统仿真的端到端工具箱汇总）  
## 研究结论与竞赛制胜策略建议  
“2026 6G/B6G Wireless Communication AI Competition”绝非一场单纯比拼深度学习模型拟合精度的传统数据科学竞赛，而是一次对下一代移动通信系统架构重塑的深度探索。综合上述分析，为在大赛中建立绝对的技术壁垒并取得优异成绩，研究人员与参赛团队应深刻贯彻以下顶层策略：  
1. **坚持模型驱动的“灰盒”架构，规避纯黑盒的收敛陷阱**：完全摒弃通信先验知识、采用纯数据驱动的“黑盒”端到端网络虽然理论自由度极高，但在存在高维信道矩阵与强噪声耦合的场景下，往往面临极难收敛、无法解释且泛化性能差的困境。最佳的系统架构是“一体化框架下的模块化内嵌”，即保留OFDM体制与基本信道特征（如物理衰落机制）的先验结构，利用深度网络替换其中难以优化的核心非线性模块（如基于深度展开技术的预编码、神经软判决接收机），最后借助STE等技术进行系统级的端到端微调优化。  
2. **重塑目标函数，深刻响应“风险感知”的计分博弈**：赛题中关于效率与公平分的计算公式不仅是一个评估工具，更是一个优化导向。单纯采用均方误差（MSE）或二元交叉熵作为损失函数将导致模型在极端恶劣信噪比下依然盲目尝试传输。团队必须设计一种“风险感知（Risk-Aware）”的定制化损失函数，将放弃传输的0.5权重补偿直接编码入优化目标中，使得发射机代理在评估到信道质量无法支撑解调时，能够智能地降低传输阶数、触发SU回退机制或彻底放弃某些资源块的分配，从而在确保极高“公平分”下限的同时稳步提升系统综合效率。  
3. **重构控制信令的语义价值，激活跨层协同**：$S_{ul}=96$ 的受限反馈资源与 $K=5$ 的极简控制比特是串联基站与终端的神经枢纽。必须抛弃将反馈链路仅仅用于还原信道矩阵参数的传统思维，转而将其视为传递“语义级调度特征”的高速公路。终端反馈应直接映射出基站最需获知的配对兼容性与干扰抵抗力指标；而基站生成的5个控制比特则需要进行精细化的协议定义，以驱动接收端无缝切换与之匹配的多套解调模型分支。  
通过系统性地掌握流形空间预编码、深度展开技术、直通估计器联合训练以及基于强化思维的多模式自适应调度技术，参赛者不仅能够构建出一套全面满足赛题苛刻约束的卓越端到端MU-MIMO传输系统，更将在推动6G原生AI从学术构想走向工业落地的进程中留下浓墨重彩的一笔。  
